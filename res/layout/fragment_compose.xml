<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.codepath.apps.mytwitterapp.ComposeActivity$PlaceholderFragment" >

<!-- Comment out for now until we have time to do overlay  
<RelativeLayout
       	android:id="@+id/rvLayout1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" >
        
       <ImageView
        android:id="@+id/ivProfileImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:maxHeight="100dp"
        android:maxWidth="100dp"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/tvTwitterName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_toRightOf="@+id/ivProfileImage"        
        android:textSize="12sp" />
    
 		 1.) Align this text as tvName
          2.) Below tvName		
     
     <TextView
        android:id="@+id/tvTwitterTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvTwitterName"
        android:layout_below="@+id/tvTwitterName"  
        android:lines="2"
        android:textSize="10sp" />
    </RelativeLayout>
     
    
    <EditText
        android:id="@+id/etTwitterMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/rvLayout1"
        android:ems="10"
        android:inputType="textMultiLine" >

        <requestFocus />
    </EditText>
-->

</RelativeLayout>
